# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: mai-transcribe-1-5
metadata:
  template: mai-transcribe-1-5@1.0.0

services:
  web:
    project: ./src
    language: js
    host: appservice
